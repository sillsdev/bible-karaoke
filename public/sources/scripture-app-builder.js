const {
  Project,
  Book,
  Chapter,
  getDirectories
} = require('./util')

const PROJECT_TYPE = 'scriptureAppBuilder'

function getProjectStructure(directories) {
  return []
}

module.exports = {
  PROJECT_TYPE,
  getProjectStructure,
};