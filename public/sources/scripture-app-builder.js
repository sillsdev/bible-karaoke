// eslint-disable-next-line no-unused-vars
const { Project, Book, Chapter, getDirectories } = require('./util');

const PROJECT_TYPE = 'scriptureAppBuilder';

// eslint-disable-next-line no-unused-vars
function getProjectStructure(directories) {
  return [];
}

module.exports = {
  PROJECT_TYPE,
  getProjectStructure,
};
